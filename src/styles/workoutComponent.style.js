import style from "./style";

const s = Object.create(style);

s.root = {
  backgroundColor: "#000000",
  minHeight: "100%",
  textAlign: "left",
};

s.container = {
  padding: 0,
};

export default s;
